describe("initial-tests-for-test", () => {
  it("test-a", () => {
    expect(1 + 1).toEqual(2);
  });

  it("test-b", () => {
    expect(true).toBeTruthy();
  });
  it("test-c", () => {
    expect(null).toBeNull();
  });
});
